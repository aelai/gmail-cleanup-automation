## Environment configuration for Gmail Cleanup Automation

# Copy this file to `.env` and fill in the required values. Never commit your real secrets to version control.

# OAuth credentials for the Gmail API.  These can be generated from
# https://console.cloud.google.com/apis/credentials.  See the README for
# instructions on enabling Gmail and People APIs.
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=

# API key or OAuth token for the Google People API.  In most cases you do
# not need a separate API key if you authenticate via OAuth.  Leave this
# blank if you plan to use the same OAuth token as above.
GOOGLE_PEOPLE_API_KEY=

# Host and model for your local LLM via Ollama.  If you are running
# Ollama on the same machine, the default endpoint below should work.
OLLAMA_ENDPOINT=http://localhost:11434
OLLAMA_MODEL=llama3

# Optional OpenAI API key if you want to fall back to ChatGPT for
# classification.  Leave empty to disable.
OPENAI_API_KEY=
